{
  "name": "OmniAuth-weirdx",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "rubocop": "git diff --name-only --diff-filter=ACMRTUXB --cached | grep '\\.rb' | grep -v db/schema.rb | xargs bundle exec rubocop"
  },
  "pre-commit": [
    "rubocop"
  ],
  "author": "shia",
  "devDependencies": {
    "pre-commit": "^1.1.2"
  }
}
